'use strict';

describe('whirlwind.task-tree-view module', function() {

  beforeEach(module('whirlwind.task-tree-view'));

  describe('task-tree-view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});