'use strict';

describe('whirlwind.task-editor-view module', function() {

  beforeEach(module('whirlwind.task-editor-view'));

  describe('task-editor-view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});